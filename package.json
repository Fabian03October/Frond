{
  "name": "cinefilos-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend-angular && npm install && npm run build:vercel",
    "dev": "cd frontend-angular && npm start"
  },
  "dependencies": {},
  "engines": {
    "node": "20.x"
  }
}
